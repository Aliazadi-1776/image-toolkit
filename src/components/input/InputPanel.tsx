import { FolderOpen, ImagePlus, Trash2 } from "lucide-react";
import { pickImageFiles, pickInputFolder } from "@/lib/fileSelection";
import { resolveInputPaths } from "@/lib/tauri";
import { useToolkitStore } from "@/stores/useToolkitStore";
export function InputPanel(){ const files=useToolkitStore((s)=>s.request.inputFiles); const setFiles=useToolkitStore((s)=>s.setFiles); const clearFiles=useToolkitStore((s)=>s.clearFiles); async function addFiles(){ const paths=await pickImageFiles(); if(paths.length) setFiles(await resolveInputPaths(paths)); } async function addFolder(){ const path=await pickInputFolder(); if(path) setFiles(await resolveInputPaths([path])); } return <section className="rounded-2xl border border-zinc-800 bg-zinc-900 p-4"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><h2 className="text-lg font-semibold">Input</h2><p className="mt-1 text-sm text-zinc-400">Add one image or a full batch.</p></div><div className="flex flex-wrap gap-2"><button type="button" onClick={addFiles} className="inline-flex items-center gap-2 rounded-xl bg-emerald-500 px-4 py-2 text-sm font-medium text-zinc-950 hover:bg-emerald-400"><ImagePlus className="h-4 w-4"/>Add images</button><button type="button" onClick={addFolder} className="inline-flex items-center gap-2 rounded-xl border border-zinc-700 px-4 py-2 text-sm text-zinc-200 hover:bg-zinc-950"><FolderOpen className="h-4 w-4"/>Add folder</button>{files.length>0&&<button type="button" onClick={clearFiles} className="inline-flex items-center gap-2 rounded-xl border border-red-900/60 px-4 py-2 text-sm text-red-200 hover:bg-red-950/30"><Trash2 className="h-4 w-4"/>Clear</button>}</div></div></section>; }
