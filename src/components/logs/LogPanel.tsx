import { useToolkitStore } from "@/stores/useToolkitStore";
export function LogPanel(){ const runSummary=useToolkitStore((s)=>s.runSummary); if(!runSummary||!runSummary.entries.length) return <section className="rounded-2xl border border-zinc-800 bg-zinc-900 p-4"><h2 className="text-lg font-semibold">Logs</h2><p className="mt-2 text-sm text-zinc-500">Run the queue to see command logs here.</p></section>; return <section className="rounded-2xl border border-zinc-800 bg-zinc-900 p-4"><h2 className="text-lg font-semibold">Logs</h2><div className="mt-4 space-y-3">{runSummary.entries.map((entry,index)=><article key={`${entry.input}-${index}`} className="rounded-xl border border-zinc-800 bg-zinc-950 p-4"><div className="flex items-center justify-between gap-3"><div className="truncate text-sm text-zinc-300">{entry.input}</div><span className={["rounded-full px-2 py-1 text-xs",entry.success?"bg-emerald-500/10 text-emerald-300":"bg-red-500/10 text-red-300"].join(" ")}>{entry.success?"success":"failed"}</span></div><pre className="mt-3 overflow-auto rounded-lg bg-black p-3 text-xs text-zinc-300">{entry.commandLine}</pre>{entry.stderr&&<pre className="mt-3 whitespace-pre-wrap rounded-lg bg-red-950/30 p-3 text-xs text-red-200">{entry.stderr}</pre>}</article>)}</div></section>; }
